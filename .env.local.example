# EUN Bot API 对接配置
# 在项目根新建 `.env.local` 并填入真实值，勿提交到 Git。

# 上游 EUN Bot API 基地址（形如 http://host:port，具体地址见内部资料）
EUN_API_BASE=

# 上游 EUN Bot API 鉴权 Token（Bearer），由后端保管，不要暴露到前端
EUN_BOT_TOKEN= 

# 网站自身管理端点的口令（用于 /api/eun/admin/* 与 /api/eun/spark/*）
# 前端触发管理动作时需带上 `x-admin-key: <该值>` 请求头
# 生产环境请替换为真实登录/鉴权体系
SITE_ADMIN_KEY=
